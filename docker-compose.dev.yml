version: "3"
services:
  kepplr-node-service:
    build:
      context: .
      args:
        NODE_ENV: development
    volumes:
      - ./:/app:ro
      - /app/node_modules
    env_file:
      - ./.env
    command: npm run start:dev